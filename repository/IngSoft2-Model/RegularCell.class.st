"
Represents a cell which has the capacity to hold a ship.
"
Class {
	#name : #RegularCell,
	#superclass : #Cell,
	#category : 'IngSoft2-Model'
}

{ #category : #'as yet unclassified' }
RegularCell >> applyEffectTo: aShip in: aBoard [

	"Por defecto, una celda regular no aplica ningún efecto."
]
